@model AlsoftShop.ViewModels.ShopViewModel

<div class="row">
    <div class="col-10 ">
        thats items list
        <ul>
            @foreach (var item in Model.Items)
            {
                <li>@item.Name @item.Price</li>
            }

        </ul>
    </div>
    <div class="col-2">
        thats shopping cart
    </div>
</div>